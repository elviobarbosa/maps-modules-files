import { Injectable } from '@angular/core';
import { MapsActionUsecase } from 'maps-angular-components';

import { Observable } from 'rxjs';
import { [PREFIX-CAMELCASE]Repository } from '../repository/[PREFIX-KABEBCASE].repository';

@Injectable({
  providedIn: 'root'
})
export class [PREFIX-CAMELCASE]ActiveUsecase extends MapsActionUsecase<
[PREFIX-CAMELCASE]ActiveBodyOutput, any> {
    
  constructor(private readonly _repository: [PREFIX-CAMELCASE]Repository) {
    super();
  }

  protected action = (data: [PREFIX-CAMELCASE]ActiveBodyOutput): Observable<any> => {
    return this._repository.activeDeactive(data);
  };
}
